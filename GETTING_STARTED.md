# Pineappleblog Editor v1.0.0

这是一个完整可运行的 v1.0.0 版本示例项目。

## 🚀 快速开始

### 前置要求

1. **Node.js** >= 18.0.0
2. **Rust** >= 1.70.0
3. **pnpm** >= 8.0.0
4. **Hugo** 或 **Zola** (需要安装到系统 PATH)

### 安装 Hugo (macOS)

```bash
brew install hugo
```

### 安装 Zola (macOS)

```bash
brew install zola
```

### 安装依赖

```bash
# 安装前端依赖
pnpm install

# 或使用 npm
npm install
```

### 开发模式

```bash
# 启动开发服务器
pnpm tauri:dev
```

这将同时启动：
- Vue 3 前端开发服务器 (http://localhost:5173)
- Tauri 应用窗口

### 构建应用

```bash
# 构建生产版本
pnpm tauri:build
```

构建完成后，可执行文件位于：
- **macOS**: `src-tauri/target/release/bundle/dmg/`
- **Windows**: `src-tauri/target/release/bundle/msi/`
- **Linux**: `src-tauri/target/release/bundle/deb/`

## 📁 项目结构

```
pineappleblog-editor/
├── src/                          # Vue 前端源码
│   ├── views/                   # 页面视图
│   │   ├── HomeView.vue         # 首页 - 创建/打开项目
│   │   ├── ProjectView.vue      # 项目管理页
│   │   └── EditorView.vue       # 编辑器页 (占位)
│   ├── stores/                  # Pinia 状态管理
│   │   └── projectStore.ts      # 项目状态
│   ├── composables/             # 组合式 API
│   │   └── useProject.ts        # 项目操作 hooks
│   ├── types/                   # TypeScript 类型
│   │   └── index.ts
│   ├── router/                  # Vue Router
│   │   └── index.ts
│   ├── App.vue                  # 根组件
│   ├── main.ts                  # 应用入口
│   └── style.css                # 全局样式
│
├── src-tauri/                    # Rust 后端
│   ├── src/
│   │   ├── engine/              # 静态引擎适配器
│   │   │   ├── traits.rs        # 统一接口
│   │   │   ├── hugo_adapter.rs  # Hugo 适配器
│   │   │   ├── zola_adapter.rs  # Zola 适配器
│   │   │   └── mod.rs
│   │   ├── models.rs            # 数据模型
│   │   ├── commands.rs          # Tauri 命令
│   │   ├── error.rs             # 错误处理
│   │   ├── utils.rs             # 工具函数
│   │   └── main.rs              # Rust 入口
│   ├── Cargo.toml               # Rust 依赖
│   └── tauri.conf.json          # Tauri 配置
│
├── package.json                  # 前端依赖
├── vite.config.ts               # Vite 配置
├── tsconfig.json                # TypeScript 配置
└── tailwind.config.js           # TailwindCSS 配置
```

## ✨ 功能特性

### 已实现功能 ✅

1. **项目管理**
   - ✅ 创建新的 Hugo/Zola 项目
   - ✅ 打开现有项目
   - ✅ 最近项目列表
   - ✅ 项目信息展示

2. **文章管理**
   - ✅ 创建新文章
   - ✅ 自动生成 Front Matter

3. **构建与预览**
   - ✅ 构建站点
   - ✅ 启动开发服务器
   - ✅ 实时预览 (通过浏览器)

4. **引擎支持**
   - ✅ Hugo 引擎适配器
   - ✅ Zola 引擎适配器
   - ✅ 统一接口抽象

### 待实现功能 🚧

1. **编辑器** (占位页面已创建)
   - Monaco Editor 集成
   - Markdown 语法高亮
   - 实时预览
   - 自动保存

2. **主题管理**
   - 主题市场
   - 主题安装
   - 主题切换

3. **部署功能**
   - GitHub Pages
   - Netlify
   - Vercel

## 🎯 使用说明

### 1. 创建新项目

1. 启动应用
2. 点击「创建新项目」
3. 填写项目信息：
   - 项目名称：`my-blog`
   - 项目路径：选择一个目录
   - 静态引擎：选择 Hugo 或 Zola
4. 点击「创建项目」

### 2. 打开现有项目

1. 点击「打开项目」
2. 选择包含 Hugo/Zola 配置文件的文件夹
3. 应用会自动识别项目类型

### 3. 创建文章

1. 进入项目后，点击「新建文章」
2. 输入文章标题
3. 文章将自动创建在 `content/posts/` 目录

### 4. 构建与预览

1. 点击顶部「构建站点」按钮
2. 等待构建完成
3. 点击「启动预览」
4. 浏览器访问 http://localhost:1313

## 🔧 配置说明

### Tauri 配置 (src-tauri/tauri.conf.json)

```json
{
  "plugins": {
    "dialog": { "all": true },  // 文件对话框
    "fs": { "all": true },       // 文件系统访问
    "shell": { "open": true }    // 打开浏览器
  }
}
```

### 安全性

- 文件系统访问受 Tauri 安全策略限制
- 仅允许打开用户选择的目录
- Shell 命令仅用于启动 Hugo/Zola 进程

## 🐛 故障排除

### Hugo/Zola 未找到

**问题**: `Hugo binary not found` 或 `Zola binary not found`

**解决方案**:
```bash
# macOS
brew install hugo
brew install zola

# 验证安装
hugo version
zola --version
```

### 端口被占用

**问题**: 预览服务器启动失败

**解决方案**:
```bash
# 查找占用端口的进程
lsof -i :1313

# 杀死进程
kill -9 <PID>
```

### 权限问题

**问题**: 无法创建项目或文件

**解决方案**:
- 确保选择的目录有写入权限
- macOS 可能需要在「系统偏好设置 > 安全性与隐私」中授权

## 📝 开发笔记

### 架构设计

- **前端**: Vue 3 + TypeScript + Pinia + TailwindCSS
- **后端**: Rust + Tauri 2.0
- **引擎**: Hugo/Zola 通过统一接口抽象

### 设计模式

1. **策略模式**: 不同引擎通过统一的 `SiteEngine` trait
2. **工厂模式**: `create_engine()` 函数创建引擎实例
3. **组合式 API**: Vue 3 Composition API 提高代码复用

### 技术亮点

- ✨ 类型安全的前后端通信
- ✨ 响应式状态管理
- ✨ 异步操作处理
- ✨ 错误处理统一
- ✨ 跨平台文件系统操作

## 📚 参考资料

- [Tauri 文档](https://tauri.app/v1/guides/)
- [Vue 3 文档](https://vuejs.org/)
- [Hugo 文档](https://gohugo.io/)
- [Zola 文档](https://www.getzola.org/)

## 📄 许可证

MIT License

---

**Made with ❤️ by Pineappleblog Team**
